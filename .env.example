# 本地部署（LLaMA-Factory API 模式，服务器上运行）
PARATERA_API_KEY=0
PARATERA_BASE_URL=http://localhost:8000/v1
CHAT_MODEL=default
EMBED_MODEL=default

# Judge 模型（外部 API，用于 LLM-as-Judge 评测）
# JUDGE_BASE_URL=https://api.deepseek.com/v1
# JUDGE_API_KEY=your-key-here
# JUDGE_MODEL=DeepSeek-V3.1

# 工具调用模式（默认文本模式，匹配 SFT 训练数据格式）
# TOOL_AGENT_PREFER_FC=0    # 设为 1 可切换到 OpenAI Function Calling 模式
